# Transcripts that are not `ready`

Held here so a directory glob cannot score them. `PROVENANCE.md` is the authority on
status; this directory just makes the authority hard to bypass by accident.

- `2004-1ED4D_Francois_Godement-Thoikyquado.txt` — mixed Vietnamese and French.
  Whole-document TCVN3 conversion turns `François` into `Franỗois`. Needs per-run
  conversion, which is the case viparse's own per-run detection exists for.
